apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: vote
  name: vote
  labels:
    tier: vote-app
spec:
  replicas: 3
  selector:
    matchLabels:
      tier: vote-app
  template:
    metadata:
      name: vote-app
      labels:
        tier: vote-app
    spec:
      containers:
      - name: vote-app
        image: dockersamples/examplevotingapp_vote
        ports:
         - containerPort: 80
